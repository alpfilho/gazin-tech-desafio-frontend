import styled from 'styled-components';

export const Container = styled.div`
	width: 100%;
	height: auto;
`;

export const Content = styled.div`
	width: auto;
	height: auto;
	padding-top: 1em;
	padding-bottom: 1em;
	display: flex;
	flex-direction: row;

	transform: translateX(calc(3em + 1.75em));
`;
