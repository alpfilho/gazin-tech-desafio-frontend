import React from 'react';

import { Title } from '@components/title';

export const ListNextReleases: React.FC<{ list: { title: string }[] }> = () => {
	return <Title>Próximos Lançamentos</Title>;
};
